// Usually, attacks happen with running system (e.g., a web server at the operating system level).

// However, as more and more systems depends on outside dependencies,
// a new surface of attacks are now available.
// 